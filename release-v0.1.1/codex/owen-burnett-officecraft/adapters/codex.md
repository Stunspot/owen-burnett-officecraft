# Codex operating adapter

Use Officecraft to govern the job; use the active Codex capability catalog to do the format-specific work. Select the narrowest applicable document, presentation, spreadsheet, PDF, connected-native, or design surface after confirming it is available in this run.

## Route the work

1. Read the Officecraft brief and the format reference matching the requested artifact.
2. Check the active capability description and its required route instructions before invoking a format surface. Treat current activation, connected sessions, and destination access as runtime facts to observe.
3. Preserve a supplied native template through its compatible native route when that route is available. For a new artifact, choose the capable local or connected production route that preserves the requested editability.
4. Create in a writable working area, preserve originals, and keep the Officefile register current for substantial work.
5. Use the selected surface's available render, export, readback, or inspection path. Record the evidence actually obtained.

## Surface-specific judgment

- For documents, favor semantic structure, editable styles, and an available page-render check.
- For presentations, use an audience-first slide map and inspect the rendered sequence or available presentation view.
- For spreadsheets, preserve formulas and data lineage; use active workbook control only when the current session and tool support it.
- For PDFs, keep their editable parent where appropriate and inspect the exported pages when an available path supports it.
- For connected Google or design work, confirm the target/copy and current account context before writes. Preserve native semantics instead of treating an import/export as equivalent by default.

## Evidence language

State separately that a capability was available, an artifact was produced, a structural check ran, a rendered or native view was inspected, or a connector confirmed a result. Ask the user for authority before an external share, upload, delivery, overwrite, account connection, or release action. When a needed surface is absent, invoke Officecraft's degraded-capability fallback and leave a source-ready production packet rather than inventing execution.
